<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhuxiaoxue.mapper.CustomerMapper">
    <insert id="save">
        INSERT INTO t_customer (name, weixin, tel, address, email, pinyin, companyname, companyid, level, type, userid)
        VALUES (#{name},#{weixin},#{tel},#{address},#{email},#{pinyin},#{companyname},#{companyid},#{level},#{type},#{userid})
    </insert>

    <select id="findByUserId" resultType="Customer">
        SELECT * FROM t_customer WHERE userid = #{userid} or userid =''
    </select>

    <select id="findByParams" resultType="Customer">
        SELECT * FROM t_customer
        <where>
            userid = #{userid} or userid =''
            <if test="keyword">
             and name like CONCAT('%',#{keyword},'%') or pinyin like CONCAT('%',#{keyword},'%')
            </if>
        </where>
        limit ${start},${length}

    </select>
    <select id="findCountByParams" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_customer
        <where>
        userid = #{userid} or userid =''
        <if test="keyword">
            and name like CONCAT('%',#{keyword},'%') or pinyin like CONCAT('%',#{keyword},'%')
        </if>
      </where>
        limit ${start},${length}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM t_customer
    </select>
</mapper>